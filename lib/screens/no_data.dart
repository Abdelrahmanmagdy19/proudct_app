import 'package:flutter/material.dart';

class NoProudectData extends StatelessWidget {
  const NoProudectData({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      backgroundColor: Colors.red,
    );
  }
}
